{
  "name": "nuxt-skew-protection-cloudflare-test",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "wrangler deploy",
    "deploy:version": "wrangler versions upload",
    "logs": "wrangler tail"
  },
  "dependencies": {
    "nuxt": "^4.0.0",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
