---
title: SkewNotification
description: Headless component for displaying update notifications.
---

A headless Vue component that provides notification logic without imposing any UI.

## Basic Usage

```vue
<template>
  <SkewNotification v-slot="{ isOpen, dismiss, reload, timeAgo }">
    <div v-if="isOpen" class="notification">
      <p>New version available! ({{ timeAgo }})</p>
      <button @click="reload">
        Reload
      </button>
      <button @click="dismiss">
        Dismiss
      </button>
    </div>
  </SkewNotification>
</template>
```

## Slot Props

The component provides these props via scoped slot:

### `isOpen`

- Type: `boolean`

Whether the notification should be shown.

Automatically set to `true` when:

- User's loaded modules are invalidated by a new deployment
- Can be manually controlled via the `open` prop

```vue
<SkewNotification v-slot="{ isOpen }">
  <div v-if="isOpen">
    Update available!
  </div>
</SkewNotification>
```

### `dismiss()`

- Type: `() => void`

Dismiss the notification (sets `isOpen` to `false`).

```vue
<SkewNotification v-slot="{ isOpen, dismiss }">
  <div v-if="isOpen">
    <p>Update available!</p>
    <button @click="dismiss">Not now</button>
  </div>
</SkewNotification>
```

### `reload()`

- Type: `() => void`

Reload the page to get the new version.

Calls `window.location.reload()`.

```vue
<SkewNotification v-slot="{ isOpen, reload }">
  <div v-if="isOpen">
    <button @click="reload">Update Now</button>
  </div>
</SkewNotification>
```

### `timeAgo`

- Type: `string`

Human-readable time since the release.

Examples: "just now", "2 minutes ago", "1 hour ago"

```vue
<SkewNotification v-slot="{ isOpen, timeAgo }">
  <div v-if="isOpen">
    <p>Released {{ timeAgo }}</p>
  </div>
</SkewNotification>
```

### `releaseDate`

- Type: `Date | null`

The release date as a Date object.

```vue
<SkewNotification v-slot="{ isOpen, releaseDate }">
  <div v-if="isOpen">
    <p>Released: {{ releaseDate?.toLocaleString() }}</p>
  </div>
</SkewNotification>
```

## Props

### `open`

- Type: `boolean`
- Default: Controlled automatically

Manually control the open state.

```vue
<script setup>
const forceOpen = ref(true)
</script>

<template>
  <SkewNotification v-slot="{ reload }" :open="forceOpen">
    <div>
      <button @click="reload">
        Update
      </button>
    </div>
  </SkewNotification>
</template>
```

## Examples

### With Nuxt UI

```vue
<template>
  <SkewNotification v-slot="{ isOpen, dismiss, reload, timeAgo }">
    <UNotification
      v-if="isOpen"
      title="Update Available"
      color="primary"
      :timeout="0"
      @close="dismiss"
    >
      <template #description>
        A new version was released {{ timeAgo }}.
        Reload to get the latest features and fixes.
      </template>

      <template #actions>
        <UButton color="primary" @click="reload">
          Reload Now
        </UButton>
        <UButton variant="ghost" @click="dismiss">
          Later
        </UButton>
      </template>
    </UNotification>
  </SkewNotification>
</template>
```

### With Custom Animation

```vue
<template>
  <SkewNotification v-slot="{ isOpen, dismiss, reload, timeAgo }">
    <Transition name="slide-up">
      <div
        v-if="isOpen"
        class="fixed bottom-4 right-4 p-4 bg-blue-500 text-white rounded-lg shadow-lg"
      >
        <div class="flex items-center gap-4">
          <div class="flex-1">
            <p class="font-semibold">
              Update Available
            </p>
            <p class="text-sm opacity-90">
              Released {{ timeAgo }}
            </p>
          </div>
          <button
            class="px-4 py-2 bg-white text-blue-500 rounded hover:bg-blue-50"
            @click="reload"
          >
            Reload
          </button>
          <button class="text-white hover:opacity-80" @click="dismiss">
            ✕
          </button>
        </div>
      </div>
    </Transition>
  </SkewNotification>
</template>

<style scoped>
.slide-up-enter-active,
.slide-up-leave-active {
  transition: all 0.3s ease;
}

.slide-up-enter-from {
  transform: translateY(100%);
  opacity: 0;
}

.slide-up-leave-to {
  transform: translateY(100%);
  opacity: 0;
}
</style>
```

### Toast Notification

```vue
<template>
  <SkewNotification v-slot="{ isOpen, reload, dismiss, timeAgo }">
    <Teleport to="body">
      <div
        v-if="isOpen"
        class="fixed top-4 right-4 max-w-sm p-4 bg-white dark:bg-gray-800 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700"
      >
        <div class="flex gap-3">
          <div class="flex-shrink-0">
            <svg class="w-6 h-6 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div class="flex-1">
            <h3 class="text-sm font-semibold text-gray-900 dark:text-white">
              Update Available
            </h3>
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
              A new version was released {{ timeAgo }}
            </p>
            <div class="mt-3 flex gap-2">
              <button
                class="text-sm font-medium text-blue-600 hover:text-blue-500"
                @click="reload"
              >
                Reload now
              </button>
              <button
                class="text-sm font-medium text-gray-600 hover:text-gray-500"
                @click="dismiss"
              >
                Dismiss
              </button>
            </div>
          </div>
        </div>
      </div>
    </Teleport>
  </SkewNotification>
</template>
```

### Banner Style

```vue
<template>
  <SkewNotification v-slot="{ isOpen, reload, dismiss }">
    <div
      v-if="isOpen"
      class="bg-gradient-to-r from-blue-500 to-purple-600 text-white"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3">
        <div class="flex items-center justify-between flex-wrap">
          <div class="flex-1 flex items-center">
            <span class="flex p-2 rounded-lg bg-white/20">
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </span>
            <p class="ml-3 font-medium">
              <span class="md:hidden">New version available!</span>
              <span class="hidden md:inline">
                A new version of this app is available. Reload to update.
              </span>
            </p>
          </div>
          <div class="order-3 mt-2 flex-shrink-0 w-full sm:order-2 sm:mt-0 sm:w-auto">
            <button
              class="flex items-center justify-center px-4 py-2 rounded-md text-sm font-medium text-blue-600 bg-white hover:bg-blue-50"
              @click="reload"
            >
              Reload
            </button>
          </div>
          <div class="order-2 flex-shrink-0 sm:order-3 sm:ml-3">
            <button
              class="-mr-1 flex p-2 rounded-md hover:bg-white/20 focus:outline-none"
              @click="dismiss"
            >
              <span class="sr-only">Dismiss</span>
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
        </div>
      </div>
    </div>
  </SkewNotification>
</template>
```

### With Auto-Reload Countdown

```vue
<script setup>
const countdown = ref<number | null>(null)
let countdownInterval: NodeJS.Timeout | null = null

function startCountdown(reload: () => void) {
  countdown.value = 10

  countdownInterval = setInterval(() => {
    if (countdown.value === null)
      return

    countdown.value--

    if (countdown.value <= 0) {
      reload()
      if (countdownInterval)
        clearInterval(countdownInterval)
    }
  }, 1000)
}

function stopCountdown() {
  if (countdownInterval) {
    clearInterval(countdownInterval)
    countdownInterval = null
  }
  countdown.value = null
}

onUnmounted(() => {
  stopCountdown()
})
</script>

<template>
  <SkewNotification v-slot="{ isOpen, reload, dismiss }">
    <div v-if="isOpen" class="notification">
      <p>New version available!</p>

      <div v-if="countdown === null">
        <button @click="() => { startCountdown(reload) }">
          Auto-reload in 10s
        </button>
        <button @click="dismiss">
          Dismiss
        </button>
      </div>

      <div v-else>
        <p>Reloading in {{ countdown }}s...</p>
        <button @click="reload">
          Reload Now
        </button>
        <button @click="stopCountdown">
          Cancel
        </button>
      </div>
    </div>
  </SkewNotification>
</template>
```

### Persistent Across Page Navigations

```vue
<template>
  <!-- Place in app.vue to persist across routes -->
  <SkewNotification v-slot="{ isOpen, reload, dismiss, timeAgo }">
    <ClientOnly>
      <Teleport to="body">
        <div v-if="isOpen" class="fixed bottom-4 right-4 z-50">
          <div class="bg-white rounded-lg shadow-xl p-4 max-w-sm">
            <p class="font-semibold">
              Update Available
            </p>
            <p class="text-sm text-gray-600">
              Released {{ timeAgo }}
            </p>
            <div class="mt-3 flex gap-2">
              <button class="btn-primary" @click="reload">
                Reload
              </button>
              <button class="btn-secondary" @click="dismiss">
                Later
              </button>
            </div>
          </div>
        </div>
      </Teleport>
    </ClientOnly>
  </SkewNotification>
</template>
```

## Behavior

The component automatically shows when:

1. A new version is deployed
2. The user's loaded modules are deleted in the new version (intelligent detection)

The component automatically hides when:

- User clicks dismiss
- User clicks reload (page reloads)
- `open` prop is set to `false`

## See Also

- [useSkewProtection](/docs/skew-protection/api/use-skew-protection) - Composable API
- [How it Works](/docs/skew-protection/guides/how-it-works) - Intelligent notification logic
- [Update Strategies](/docs/skew-protection/guides/update-strategies) - Configure detection
