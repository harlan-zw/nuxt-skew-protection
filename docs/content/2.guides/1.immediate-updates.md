---
title: Update Notifications
description: React to version updates using useSkewProtection() composable.
relatedPages:
  - path: /docs/skew-protection/api/use-skew-protection
    title: useSkewProtection()
  - path: /docs/skew-protection/nitro-api/is-client-outdated
    title: isClientOutdated()
---

Using the [update strategies](/docs/skew-protection/guides/update-strategies) the module knows when
you deploy a new version. You can use the built-in [`<SkewNotification>`{lang="html"}](/docs/skew-protection/api/skew-notification) component or build your own using the
[`useSkewProtection()`{lang="ts"}](/docs/skew-protection/api/use-skew-protection) composable.

Use [`onAppOutdated()`{lang="ts"}](/docs/skew-protection/api/use-skew-protection#onappoutdated) when you need custom behavior. It runs whenever Nuxt receives a newer deployment manifest.

## Setting Up Notifications

The `<SkewNotification>`{lang="html"} component is a client-only wrapper around the `useSkewProtection()`{lang="ts"} composable.

When a new deployment is available:

```vue [app.vue]
<template>
  <SkewNotification v-slot="{ isAppOutdated }">
    <Transition name="slide-up">
      <div v-if="isAppOutdated" class="fixed bottom-4 left-1/2 transform -translate-x-1/2 bg-blue-600 text-white px-4 py-2 rounded shadow">
        An update is available. Please refresh the page.
      </div>
    </Transition>
  </SkewNotification>
</template>
```

Please see the [UI Examples](/docs/skew-protection/guides/ui-examples) guide for more user friendly notification patterns.

## Server-Side Skew Detection

Detect outdated clients in server handlers using [`isClientOutdated()`{lang="ts"}](/docs/skew-protection/nitro-api/is-client-outdated):

```ts [server/api/data.ts]
import { isClientOutdated } from 'nuxt-skew-protection/server'

export default defineEventHandler((event) => {
  if (isClientOutdated(event)) {
    setResponseStatus(event, 409)
    return { error: 'Client outdated', requiresReload: true }
  }

  return { data: '...' }
})
```
