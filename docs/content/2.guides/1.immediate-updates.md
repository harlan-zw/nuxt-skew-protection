---
title: Update Notifications
description: React to version updates using useSkewProtection() composable.
relatedPages:
  - path: /docs/skew-protection/api/use-skew-protection
    title: useSkewProtection()
  - path: /docs/skew-protection/nitro-api/is-client-outdated
    title: isClientOutdated()
---

The module knows when you deploy a new version. It shows a built-in update notification by default.
Users can refresh the app or dismiss the notification.

The default `reloadStrategy: 'prompt'` setting enables this interface. Add
[`<SkewNotification>`{lang="html"}](/docs/skew-protection/api/skew-notification) to replace it.
You can also build custom behaviour with [`useSkewProtection()`{lang="ts"}](/docs/skew-protection/api/use-skew-protection).

Update notifications can be an annoying user experience if not handled properly. The module
provides two hooks to differentiate between deployments:
- [`onAppOutdated()`{lang="ts"}](/docs/skew-protection/api/use-skew-protection#onappoutdated) - triggers whenever it detects a new version.
- [`onCurrentChunksOutdated()`{lang="ts"}](/docs/skew-protection/api/use-skew-protection#oncurrentchunksoutdated) - triggers only when the user's loaded chunks are invalidated by the new deployment.

## Custom Notifications

The `<SkewNotification>`{lang="html"} component is a client-only wrapper around `useSkewProtection()`{lang="ts"}.
If it mounts, the module hides its built-in notification.

When the user's current chunks are outdated:

```vue [app.vue]
<template>
  <SkewNotification v-slot="{ isCurrentChunksOutdated }">
    <Transition name="slide-up">
      <div v-if="isCurrentChunksOutdated" class="fixed bottom-4 left-1/2 transform -translate-x-1/2 bg-blue-600 text-white px-4 py-2 rounded shadow">
        An update is available. Please refresh the page.
      </div>
    </Transition>
  </SkewNotification>
</template>
```

See [Notification UI](/docs/skew-protection/guides/ui-examples) for more patterns.

## Server-Side Skew Detection

Detect outdated clients in server handlers using [`isClientOutdated()`{lang="ts"}](/docs/skew-protection/nitro-api/is-client-outdated):

```ts [server/api/data.ts]
import { isClientOutdated } from 'nuxt-skew-protection/server'

export default defineEventHandler((event) => {
  if (isClientOutdated(event)) {
    setResponseStatus(event, 409)
    return { error: 'Client outdated', requiresReload: true }
  }

  return { data: '...' }
})
```
